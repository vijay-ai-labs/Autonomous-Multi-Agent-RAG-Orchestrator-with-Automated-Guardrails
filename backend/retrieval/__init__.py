"""SP2 Phase 1 — retrieval engine (embed, hybrid search, rerank, verify).

The only stable public entry point is :func:`retrieval.service.retrieve`, which
SP2 Phase 2 (answer generation) calls directly.
"""
