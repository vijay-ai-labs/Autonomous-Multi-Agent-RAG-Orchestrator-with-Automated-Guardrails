fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
qdrant-client>=1.9
redis>=5.0
pydantic-settings>=2.2
python-jose[cryptography]>=3.3
python-multipart>=0.0.9
passlib[bcrypt]>=1.7
# passlib 1.7.4 predates the bcrypt 4.1+/5.x API; bcrypt 5 hard-raises on its
# version self-test ("password cannot be longer than 72 bytes"). Pin to the last
# compatible line until passlib ships a fix.
bcrypt<4.1
httpx>=0.27

# Phase 2 — document ingestion (parsing + chunking)
pypdf>=4.0
python-docx>=1.1
beautifulsoup4>=4.12
lxml>=5.0
tiktoken>=0.7
pytest>=8.0
pytest-asyncio>=0.23
reportlab>=4.0

# Phase 3 — embedding, indexing, async worker
fastembed>=0.3
celery>=5.4
openai>=1.30

# SP2 Phase 1 — retrieval (cross-encoder reranking)
flashrank>=0.2

# SP3 Phase 1 — LangGraph agent graph
langgraph>=0.2

# SP3 Phase 2 — guardrails (PII detection)
presidio-analyzer>=2.2

# SP4 — eval suite
ragas>=0.1
datasets>=2.19
